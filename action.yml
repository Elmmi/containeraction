name: 'my first container action'
description: 'Tech God'
author: 'Elemi Ani'
inputs:
  myInput:
    description: 'greeting to use'
    required: true
    default: ; Awesome guru'
outputs:
  myOutput:
    description: 'total memory'
runs:
  using: 'docker'
  image: 'Dockerfile'
